import {VerticalTimelineElementProps} from 'react-vertical-timeline-component';

export interface TimeLineItem extends VerticalTimelineElementProps {
  title?: String;
  description?: String;
  regency?: String;
  url?: String;
}
